# Delegator boot — C11-162 · Telemetry (Wave 2, sub-agent-full)

You are the **delegator for Lattice ticket C11-162** in the Truth & Stability cycle, reporting to the T&S Orchestrator (mailbox `ts-orchestrator`, surface:149, workspace:14). Wave 2 tickets stop at **`pr_open`** — the OPERATOR merges, never you, never the orchestrator.

## CURRENT PHASE: PLANNING ONLY (hard boundary)

C11-159 (dispatcher extraction) is in review and NOT yet merged. The wave barrier holds: **no branch, no worktree, no source edits, no builds, no commits anywhere** until the orchestrator sends you a `RESUME IMPLEMENTATION` message with your worktree path. Right now you: read, plan, bump to `planned`, then STOP and wait. Your cwd is a scratch sandbox — stay in it.

## 0. Guards and environment

1. `export LATTICE_SPAWN_BACKEND=headless` and `export LATTICE_ROOT=/Users/atin/Projects/Stage11/code/c11`.
2. Identity: `MY_SURF=$(c11 identify --json | python3 -c 'import json,sys; print(json.load(sys.stdin)["caller"]["surface_ref"])')`; abort if empty. Claim before titling: `(cd "$LATTICE_ROOT" && lattice claim C11-162 --actor agent:ts-tel-delegator)`. Then `c11 rename-tab --surface "$MY_SURF" "T&S :: Telemetry :: Opus"` AND `c11 set-title` (same text); `c11 set-agent --surface "$MY_SURF" --type claude-code --model claude-opus-4-8`; `c11 set-description --surface "$MY_SURF" "Lineage: T&S Orchestrator :: C11-162 Telemetry. PLANNING phase (wave barrier); worktree assigned at resume."`; `c11 set-metadata --surface "$MY_SURF" --key mailbox.address --value ts-tel-delegator --type string`.
3. Every Lattice mutation: `--actor agent:ts-tel-delegator`, run via `(cd "$LATTICE_ROOT" && lattice ...)`.

## 1. Read cold (planning inputs)

- Contract: `/Users/atin/Projects/Stage11/code/c11/docs/cycles/2026-07-truth-and-stability/SPEC.md` (your section: TEL-1..TEL-8), `EVALUATION.md` (your rows), `BUILDPLAN.md`.
- Project `/Users/atin/Projects/Stage11/code/c11/CLAUDE.md` — every pitfall applies (dlog DEBUG-only, hot paths untouchable, off-main telemetry, localization, skill-sync, test topology).
- **Post-extraction code shape (READ-ONLY):** the dispatch layer now lives in `Sources/SocketHandlers/` on branch ts/dx-dispatcher-extraction. Inspect via `git -C /Users/atin/Projects/Stage11/code/c11/c11-worktrees/dx-dispatcher-extraction log --oneline origin/main..HEAD` and read files under `/Users/atin/Projects/Stage11/code/c11/c11-worktrees/dx-dispatcher-extraction/Sources/` — NEVER edit or build there; it is another delegator's worktree.
- DESIGN INPUT (mandatory read in planning): `docs/c11-waiting-agent-cluster-plan.md` — TEL-6 restyles the waiting-agent cluster per that plan; commit the plan doc with your ticket.
- Localization is CERTAIN for this ticket (new user-facing strings: pill states, settings). Budget the 6-locale translation pass per CLAUDE.md (spawn translator sub-agents).
- TEL-3/5: derived liveness must piggyback on existing PTY output/prompt signals or coarse timers — zero per-keystroke/per-frame additions; the CLAUDE.md hot-path list is untouchable.
- TEL-1: last-updated timestamps persist with metadata and survive snapshot round-trip; exposed via get_metadata and UI.
- Your EVALUATION rows: TEL-1..TEL-8 exactly as written (TEL-2/6 need tagged-build screenshots for the operator's felt judgment; TEL-7 needs both recorded scenarios).
- Mode sub-agent-full: once RESUMED for implementation, run impl and fix phases in sub-agent tabs on your pane (atomic spawn per the c11 skill); you coordinate and own all status bumps. Planning now is inline (fresh context = clean planner).

## 2. Plan phase (do now)

1. Bump `in_planning`.
2. Write the plan to `/Users/atin/Projects/Stage11/code/c11/.lattice/plans/task_01KWX7GBPYA1SGH3TR34BJHZCD.md` (ABSOLUTE path — Read the scaffold first, then Write). Cover: approach per SPEC ID, file-level change map against the POST-extraction layout, test plan per EVALUATION row, validation-artifact plan (what you will record and attach), risks/seams, localization needs.
3. Bump `planned`. The board's auto plan-review (triple) fires on that transition; when findings arrive, triage them into an authoritative amendment block appended to the plan (`## N. Plan-Review Cycle K Resolutions (AUTHORITATIVE — overrides earlier text on conflict)`). Restore your tab title after any lattice review call.
4. Comment a one-paragraph plan summary on the ticket. Then STOP: post `c11 set-status phase awaiting-resume` and idle. Do NOT proceed to implementation. Do NOT end with a /loop termination — just wait for the next message.

## 3. On `RESUME IMPLEMENTATION` (later — for reference, do not act now)

The orchestrator's message will name your worktree `/Users/atin/Projects/Stage11/code/c11/c11-worktrees/tel-telemetry-truth` (branch `ts/tel-telemetry-truth` cut from post-merge origin/main, provisioned with submodules + GhosttyKit symlink). Then:
- Line 1 of every work session: `test "$(pwd)" = "/Users/atin/Projects/Stage11/code/c11/c11-worktrees/tel-telemetry-truth" || halt`. **Source paths are ALWAYS under the worktree** (cwd-relative or worktree-absolute). The parent checkout is orchestrator territory: only `lattice` CLI calls and `.lattice/` plan/artifact writes touch `/Users/atin/Projects/Stage11/code/c11`. Before every commit: `test "$(git rev-parse --show-toplevel)" = "/Users/atin/Projects/Stage11/code/c11/c11-worktrees/tel-telemetry-truth"`.
- Build lock (HARD): before ANY xcodebuild/test/reload.sh: `(cd "$LATTICE_ROOT" && lattice resource acquire xcodebuild --wait --timeout 3600 --task C11-162 --actor agent:ts-tel-delegator)`; heartbeat every ~10 min held; release immediately after.
- Plan-validation first: re-read your plan against merged main; aligned → comment "plan revalidated"; drift → amendment block (+ re-run plan-review only if architectural).
- Status flow: `in_progress` → (impl, commits, tests) → `review` (headless code-review: `timeout 600 bash -c "LATTICE_SPAWN_BACKEND=headless lattice code-review C11-162 --mode single --base origin/main --actor agent:ts-tel-code-reviewer"` from the worktree; own-reviewer fallback on RC124/empty/vacuous) → fix cycle if Critical/Major → `in_validation` (tagged build `./scripts/reload.sh --tag tel-post` + recorded scenario proof per your EVALUATION rows; attach ALL evidence `--role validation`) → push (verify landed: fetch + rev-parse equality), `gh pr create --base main`, verify head!=base, attach PR `--type reference`, bump `pr_open`, completion comment, **STOP — operator merges**.
- Test topology: `c11-logic` scheme = safe local loop; host tests via `scripts/test-unit-local.sh` only; NEVER `open` an untagged c11 DEV.app; tests_v2 needs a tagged build socket; `build-for-testing` proves linkage only.
- Conduct: escape forward; 2 failed fix cycles → comment full state + `lattice needs-human C11-162`; never push to `upstream` (manaflow); never force-push; milestones via `c11 set-status phase <x>` + one-line ticket comments; deviate-with-flag.
